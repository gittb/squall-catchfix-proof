# notes

A tiny in-memory Notes API (FastAPI). Throwaway repo used to prove Squall's
merge-ceremony catch-and-fix loop end-to-end (a deliberately-bad PR is caught
by review, routed to a fix-worker, re-reviewed, and merged).
